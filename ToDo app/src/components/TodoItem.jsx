//Delete button

export const TodoItem = ({ todo }) => {
  return (
    <div>
      {todo}
      <button>Delete</button>
    </div>
  );
};
//Toggle button
export const TodoItem = ({ todo }) => {
  return (
    <div>
      {todo}
      <button>Delete</button>
    </div>
  );
};
